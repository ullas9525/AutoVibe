name: autovibe
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  android_alarm_manager_plus: ^5.0.0
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  uuid: ^4.5.2
  native_ringer:
    path: packages/native_ringer

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
